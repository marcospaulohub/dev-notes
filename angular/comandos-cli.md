# 🛠 Comandos Angular CLI

### 🔍 Informações

Verifica a versão do node.
```bash
node --version 
# ou
node -v 
```

Verifica a versão do npm.
```bash
npm --version 
# ou
npm -v 
```

Para instalar o Angular CLI. 
```bash
 npm install -g @angular/cli
 # -g = Instala de forma global
```

Para instalar o Angular CLI em uma versão específica.
```bash
 npm install -g @angular/cli@<versão>
 npm install -g @angular/cli@19.0.2
 # @ = utilzado para específicar a versão.
```

Para validar a versão instalada.
```bash
 ng --version
``` 

Para criar um novo projeto.
```bash
 ng new <nome-projeto>
 #
 ng new conteitos-basicos
``` 

Para executar o projeto.
```bash
 ng serve 
 ng serve --open # executa o projeto e abre o navegador.
``` 




<!-- 

```bash
 
``` 
-->


<!-- ng new projeto  
ng serve   -->
