# 🧩 Componentes
### Comando para criar um componente

O comando `ng g c nome-do-componente` é usado no Angular para gerar um novo componente.

- `ng`: Este é o comando da CLI do Angular.
- `g`: Esta é uma abreviação para `generate` (gerar).
- `c`: Esta é uma abreviação para `component` (componente).
- `nome-do-component`: Este é o nome que você deseja dar ao seu novo componente.

**Exemplo**:<br>
Para criar um novo componente chamado `minha-pagina`, você usaria o seguinte comando:
```bash
ng g s <nome-do-componente>
ng g s minha-pagina
``` 
---