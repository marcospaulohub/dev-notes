# 🗂️ Serviços
### Comando para criar um serviço

O comando `ng g s nome-do-service` é usado no Angular para gerar um novo serviço.

- `ng`: Este é o comando da CLI do Angular.
- `g`: Esta é uma abreviação para `generate` (gerar).
- `s`: Esta é uma abreviação para `service` (serviço).
- `nome-do-servico`: Este é o nome que você deseja dar ao seu novo servidor.

**Exemplo**:<br>
Para criar um novo serviço chamado `cliente-service`, você usaria o seguinte comando:
```bash
ng g s <nome-do-service>
ng g s cliente-service
``` 
---